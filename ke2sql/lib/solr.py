#!/usr/bin/env python
# encoding: utf-8
"""
Created by Ben Scott on '04/09/2017'.
"""


def solr_get_schema(dataset):
    print(dataset)
    pass


def solr_get_query(dataset):

    print(dataset.fields)

    # coldefs = ','.join(
    #     '{name} {type}'.format(name=col.field_name, type=col.field_type) for col in self.columns
    # )

    pass
